<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apisense.bee" >

    <!-- BEGIN APISENSE SETTINGS -->
    <uses-feature android:name="android.hardware.camera"/>
    <!-- GCM permission-->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Used to store script in sdcard folder -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!-- Used to access to apisense remote service -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <!-- ***** Optional permission -->
    <uses-permission android:name="android.permission.READ_LOGS"/>
    <!-- location permission -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_UPDATES"/>
    <!-- phone permission -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- ? permission -->
    <uses-permission android:name="android.permission.GET_TASKS"/>
    <!-- <uses-permission android:name="android.permission.INSTALL_PACKAGES"/> -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- END APISENSE SETTINGS -->

    <application
        android:name=".BeeApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".ui.activity.SlideshowActivity"
            android:label="@string/title_activity_slideshow" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.HomeActivity"
            android:label="@string/title_activity_home" >
        </activity>
        <activity
            android:name=".ui.activity.StoreActivity"
            android:label="@string/title_activity_store" >
        </activity>
        <activity
            android:name=".ui.activity.CollectDetailsActivity"
            android:label="@string/title_activity_collect_details" >
        </activity>


        <service android:name="fr.inria.bsense.service.BeeSenseServiceManager"
            android:enabled="true"/>
    </application>

</manifest>
